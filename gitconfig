[user]
	name = Danny Cosson
	email = dcosson@gmail.com
[core]
	excludesfile = /Users/dcosson/.gitignore
[rerere]
	enabled = 1
[branch "master"]
	remote = origin
	merge = refs/heads/master
[alias]
        br = branch
	ci = commit -a
	co = checkout
	dwc = diff -w -M --color-words
	fix = rebase -i origin/master
	branch-ages = for-each-ref --format='%(committerdate:relative)%09%(refname:short)' --sort='-committerdate' refs/heads
	branch-ages-origin = for-each-ref --format='%(committerdate:relative)%09%(refname:short)' --sort='-committerdate' refs/remotes/origin
	re = remote
	st = status
	sup = "submodule foreach 'git co master; git up'"
	tree = log --graph --decorate --pretty=oneline --abbrev-commit --all
	up = pull --rebase
	everything = log --pretty=format: --name-only --diff-filter=A
[push]
	default = simple
[merge]
	defaultToUpstream = true
[color]
	ui = true
